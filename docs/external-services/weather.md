# External Weather Service

The Scheduler uses an external [Weather service](https://github.com/gemini-hlsw/weather) to handle weather conditions changes
that could happen in the night. For VALIDATION this data is retrieved from files (check the folder `services/environment/`).